# Git Workflow

- `main` is the deployable branch.
- Use feature branches named `feat/short-description`.
- Use Conventional Commits.
- Pull requests should include tests, docs updates, and security notes for relevant changes.
- Solo project exception: self-merge is allowed during MVP development, but production changes should be reviewed.
